import React from "react";
import { FileProps } from "..";

export const type = 5;
export const exts = [];

const Markdown = ({ file }: FileProps) => {
  return <div>markdown</div>;
};

export default Markdown;